var teacherMenuTreeEval = {
  evaluator: {
    id: 0,
    // double-digit to avoid conflicts with adminMenuTree
    key: 'evaluator',
    menuName: 'topNav.primaryMenu.evaluator',
    route: '/evaluator',
    subMenu: [],
    isActiveFor: [] // routes this menu is active for

  }
};
export default teacherMenuTreeEval;