import { makeStyles } from 'tss-react/mui';
export default makeStyles({
  name: 'StudentAssignmentsTableContainer'
})(function () {
  return {
    description: {
      marginBottom: "var(--ebl-s4)"
    }
  };
});