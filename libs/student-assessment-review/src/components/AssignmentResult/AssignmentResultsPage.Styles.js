import { makeStyles } from 'tss-react/mui';
export default makeStyles({
  name: 'StudentAssessmentReview'
})(function () {
  return {
    header: {
      marginBottom: 'var(--ebl-page-header-margin-bottom)'
    }
  };
});