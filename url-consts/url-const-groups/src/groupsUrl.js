/* eslint-disable import/prefer-default-export */
var TEACHER_GROUPS = 'groups'; // TODO: Remove "my-classes" version (used in Ed) when we switch to AMP

var MY_CLASSES = 'my-classes';
export var TEACHER_GROUPS_URL = "".concat(MY_CLASSES, "/").concat(TEACHER_GROUPS);
export var TEACHER_GROUPS_AMP_URL = TEACHER_GROUPS;